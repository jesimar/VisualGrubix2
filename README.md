# Visualizador RSSF em Python

Preparando o ambiente

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

Executando a aplicação

```bash
python manage.py runserver
```
